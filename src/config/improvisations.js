/**
 * Improvisasi di luar brief (sudah kamu setujui, 24-09-2026).
 * Detail tiap improvisasi ada di docs/improvisasi.md.
 * Ubah ke `false` kalau suatu saat mau dimatikan lagi.
 */
export const IMPROV = {
  // Page 1: tombol "Y" makin besar tiap kali "G" kabur + balon ejekan kecil di dekat "G".
  p1Teasing: true,

  // Page 7: tap/klik foto polaroid → foto tampil besar (lightbox).
  p7Lightbox: true,

  // Page 11: tombol kecil "Ulang dari awal ↺" di paling bawah.
  p11ReplayButton: true,

  // Page 4: setelah kado terbuka, muncul kue dengan 23 lilin yang ditiup (tap).
  p4Cake: true,

  // Page 10: photobooth kamera (selfie dengan bingkai & stiker, hasil disimpan di HP Vina).
  p10Photobooth: true,
}
